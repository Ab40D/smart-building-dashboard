# Profile photo

Drop your profile photo at `public/images/profile.jpg` (or update the path in
`src/data/profile.ts`).

The site gracefully falls back to a monogram placeholder if the file is missing,
so nothing breaks while you're working on it.
